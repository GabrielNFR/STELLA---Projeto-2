{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*", "/media/*", "/static/*", "*.{css,js,png,jpg,svg,ico,woff,woff2}"]
  },
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous"]
    }
  ],
  "globalHeaders": {
    "X-Content-Type-Options": "nosniff",
    "X-Frame-Options": "DENY"
  }
}
